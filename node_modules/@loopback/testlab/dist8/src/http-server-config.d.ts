/**
 * Helper function for generating Travis-friendly host (127.0.0.1)
 * @param options Optional defaults
 */
export declare function givenHttpServerConfig<T>(options?: T): T & {
    host?: string;
};
